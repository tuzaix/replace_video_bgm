# 视频BGM分离和替换工具 v3.0 依赖

# 深度学习框架
torch>=1.12.0
torchaudio>=0.12.0

# 音频分离库
demucs>=4.0.0

# 视频处理库
moviepy>=1.0.3

# 音频处理库
librosa>=0.9.0
soundfile>=0.10.0

# 科学计算库 (v3.0新增)
scipy>=1.9.0
numpy>=1.21.0

# 其他工具库
pathlib2>=2.3.0
tqdm>=4.64.0

# GPU支持（可选，如果有NVIDIA GPU）
# 取消注释下面的行来安装CUDA版本的PyTorch
# torch>=1.12.0+cu116
# torchaudio>=0.12.0+cu116
# --extra-index-url https://download.pytorch.org/whl/cu116

# 开发和测试依赖（可选）
# pytest>=7.0.0
# pytest-cov>=4.0.0

# 可选后端（更精准的人脸识别与性别/美观评估）
# 如需启用，请取消注释相应行并安装。
# MediaPipe（更稳健的人脸检测，性别仍采用启发式）：
# mediapipe>=0.10.0
# InsightFace（性别预测，需要ONNX运行时）：
# insightface>=0.7.3
# onnxruntime>=1.16.0  # 或 onnxruntime-gpu（如需GPU推理）


opencv-python-headless
numpy 
ultralytics 
deepface